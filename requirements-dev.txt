# Development and CI tooling only. Never installed in production.
#   pip install -r requirements.txt -r requirements-dev.txt

# Linting / formatting
ruff>=0.6,<1.0

# Security
pip-audit>=2.7,<3.0
bandit>=1.7,<2.0

# Testing
coverage>=7.4,<8.0

# Fixtures for the document pipeline tests (synthetic scanned PDFs).
# Needed by the OCR work package; harmless before then.
reportlab>=4.0,<5.0
Pillow>=12.0,<13.0
# WP7: pure-Python .po->.mo compiler (the Windows dev image has no GNU gettext).
babel==2.17.0

# WP10: deploy-contract test parses docker-compose.prod.yml + the CI workflow (pure-Python, Windows-safe).
PyYAML==6.0.2
